/**
 * @param {number[]} coins
 * @param {number} amount
 * @return {number}
 */
var coinChange = function(coins, amount) {
    let n=coins.length;
    let f=[];
    for(let i=0;i<n;i++){
        f[i]=[];
        for(let m=0;m<=amount;m++){

        }
    }


};
